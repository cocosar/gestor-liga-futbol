// Exportamos todos los mocks desde un único punto de entrada
export * from './ui-components';

// Re-exportamos inicializeMocks como función principal
export { initializeMocks } from './ui-components'; 